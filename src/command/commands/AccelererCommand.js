import { Command } from '../Command.js';

export class AccelererCommand extends Command {
  constructor(pilote) {
    super();
    this.pilote = pilote;
  }

  execute() {
    // TODO
  }

  undo() {
    // TODO
  }
}
